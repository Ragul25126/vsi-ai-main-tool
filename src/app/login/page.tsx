import { LoginPage } from "@/components/auth/LoginPage";

export const metadata = {
  title: "Login - VSI AI Suite",
  description: "Sign in to VSI AI Suite GEO Platform",
};

export default function Page() {
  return <LoginPage />;
}
